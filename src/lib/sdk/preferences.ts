/** Preferences that are shared on every device a user logs in on. */
export interface AccountPreferences
{
	/** If true, the user prefers for forum threads to show the newest posts first. */
	threadsShowNewestPostsFirst?: boolean
}
